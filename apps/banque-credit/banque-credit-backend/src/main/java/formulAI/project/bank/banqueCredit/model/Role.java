package formulAI.project.bank.banqueCredit.model;

/**
 * Role applicatif d'un utilisateur (ENT-004). Un utilisateur possede exactement un role.
 */
public enum Role {
    CONSEILLER,
    RESPONSABLE_CREDIT
}

